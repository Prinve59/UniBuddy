services:
  - type: web
    name: UniBuddy
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "daphne UniBuddy.asgi:application --bind 0.0.0.0 --port 8000"
    healthCheckPath: "/"
    ports:
      - 8000
